/*
 * pwix:cookie-manager/src/client/js/client.js
 */

CookieManager._client = {
    // the button option the user has chosen, defaulting to accept
    chosenButton: null,
    defaultButton: 'accept'
};
